# Implementation Plans

Generated by the improve skill on 2026-06-30 (commit `894eabc`). Execute in
the order below. Each executor: read the plan fully before starting, honour its
STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Dynamic homepage service cards (CPT query) | P2 | M | — | DONE (commit f665d99 + BOM fix 3534c55) |
| 002  | Dynamic homepage portfolio (CPT query) | P2 | M | — | DONE (commit f665d99 + BOM fix 3534c55) |

Plans 001 and 002 are independent — they both edit `front-page.php` but in
different sections. Run them sequentially to avoid conflicts, or patch both
sections in a single session.

## Dependency notes

- 001 and 002 touch different non-overlapping sections of `front-page.php`
  (001 = services-grid; 002 = portfolio-grid). They can be done in either
  order, but do not run them in parallel on the same branch.

## Findings considered and rejected

- **#5 (no empty state on /projects/ page)**: Low priority during initial
  setup phase; deferred, not planned.
- **#6 (dead nonce in contact form)**: The root issue (broken mailto: form)
  is tracked separately as finding #1. Fix the form handler first; the nonce
  cleanup is a consequence, not independent work.
